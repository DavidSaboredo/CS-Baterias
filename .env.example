# Production/preview database URL (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# Optional API key for /api/public/products endpoints
# STOCK_API_KEY="replace-with-a-secure-random-string"

# Optional CORS allow-list for external stock API consumers
# STOCK_API_ALLOWED_ORIGINS="https://shop.example.com,https://admin.example.com"
